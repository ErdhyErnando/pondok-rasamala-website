---
import Base from "../../layouts/Base.astro";
import PhotoPlaceholder from "../../components/ui/PhotoPlaceholder.astro";
import SectionHeader from "../../components/ui/SectionHeader.astro";
import { posts, postPath } from "../../data/posts";
import { localizePath } from "../../lib/i18n";

export const prerender = true;

const locale = "id" as const;
const L = (p: string) => localizePath(p, locale);
const copy = {
	title: "Blog",
	eyebrow: "Cerita & panduan",
	meta: "Blog Pondok Rasamala: panduan camping, villa, curug, dan cerita dari kaki Gunung Salak.",
	description: "Panduan praktis dan cerita dari kaki Gunung Salak.",
	read: "Baca",
};
---

<Base title={copy.title} description={copy.meta} locale={locale}>
	<div class="container page">
		<SectionHeader
			eyebrow={copy.eyebrow}
			title={copy.title}
			description={copy.description}
		/>
		<div class="post-grid">
			{posts.map((p) => (
				<article class="post-card reveal">
					<a href={L(postPath(p.slug))} class="card-visual" aria-hidden="true" tabindex="-1">
						<PhotoPlaceholder label={`${p.title.id} — gambar menyusul`} ratio="16 / 10" />
					</a>
					<p class="card-eyebrow">{p.category.id}</p>
					<h2><a href={L(postPath(p.slug))}>{p.title.id}</a></h2>
					<p class="card-excerpt">{p.excerpt.id}</p>
					<a href={L(postPath(p.slug))} class="card-link">{copy.read} →</a>
				</article>
			))}
		</div>
	</div>
</Base>

<style>
	.page {
		padding-top: var(--section-pad);
		padding-bottom: var(--section-pad);
	}
	.post-grid {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: var(--spacing-2xl) var(--spacing-xl);
	}
	.card-visual {
		display: block;
	}
	.card-visual :global(.photo-placeholder) {
		border-radius: var(--radius);
	}
	.card-eyebrow {
		margin-top: var(--spacing-md);
		font-size: var(--font-size-xs);
		font-weight: 700;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--color-brand);
	}
	.post-card h2 {
		margin-top: var(--spacing-xs);
		font-size: var(--font-size-xl);
		line-height: 1.35;
	}
	.post-card h2 a:hover {
		color: var(--color-brand);
	}
	.card-excerpt {
		margin-top: var(--spacing-xs);
		font-size: var(--font-size-sm);
		color: var(--color-muted);
		line-height: var(--leading-relaxed);
	}
	.card-link {
		display: inline-block;
		margin-top: var(--spacing-sm);
		font-size: var(--font-size-sm);
		font-weight: 600;
		color: var(--color-brand);
	}
	@media (max-width: 960px) {
		.post-grid {
			grid-template-columns: repeat(2, 1fr);
		}
	}
	@media (max-width: 600px) {
		.post-grid {
			grid-template-columns: 1fr;
		}
	}
</style>
